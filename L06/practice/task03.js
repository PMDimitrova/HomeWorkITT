let arr = ['a', 'b', 'c'];
let char = 'c';

let indexOfChar = arr.indexOf(char);

console.log(indexOfChar === -1 ? "the array DOES NOT contain the char" : "the array contains the char on index " + indexOfChar);
